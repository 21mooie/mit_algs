import { Graph } from "../../utils/Graph";
import { GraphNode } from "../../utils/Node";

const dijkstra = (graph :Graph, source :GraphNode, target :GraphNode) => {
    return 'hello world';
};


export default dijkstra;