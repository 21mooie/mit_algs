import dijkstra from "../lectures/lecture16/dijkstra";

describe('Dijkstra', () => {
    it('should be able to return the shortest path from this graph.', () => {
        // dijkstra
        expect(1+1).toEqual(2);
    });
});